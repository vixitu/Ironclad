mixin category(category)
    li.list-group-item(id=category.name) #[i(class=category.icon)] #{category.name}

mixin command(command)
    li.list-group-item(class=command.category) #[i(class=command.icon)] #{command.name}